export const dbName = "todo-app";

export const appThemes = {
    light: "theme-light",
    dark: "theme-dark",
};

export const defaultTheme = appThemes.dark;

// DB CONSTANTS
export const THEME = "theme";